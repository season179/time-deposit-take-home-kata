/**
 * Shared Constants
 * 
 * Centralized constants used throughout the application to avoid duplication.
 */

/**
 * Milliseconds in one day (24 hours)
 * Used for date calculations across the application
 */
export const MS_PER_DAY = 1000 * 60 * 60 * 24
